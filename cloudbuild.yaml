steps:
  - name: gcr.io/cloud-builders/docker
    args: ['build', '-t', 'us-docker.pkg.dev/$PROJECT_ID/REPO/IMAGE:$COMMIT_SHA', '.']

images:
  - us-docker.pkg.dev/$PROJECT_ID/webgame/webgame-server:$COMMIT_SHA

options:
  logging: CLOUD_LOGGING_ONLY